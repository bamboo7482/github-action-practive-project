name: Output information
on: 
  issues:
    types: [opened, edited, closed]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub context
        run: echo "${{ toJSON(github.event.issue) }}" # get info của github ra dạng JSON